let n1 = Number(window.prompt('Digite um Numero:'))  // (Number.parseInt) ou so (Number) converte o item para um numero
let n2 = Number(window.prompt('Digite outro Numero:'))
let s = n1 + n2 

window.alert('A soma dos valores é ' + s) // Lembrando que o simbolo do mais e igual a juntar (concatenar) nao a somar
